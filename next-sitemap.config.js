/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: 'https ://localhost:3000', // Substitua pelo URL do seu site
    generateRobotsTxt: true, // Gera o arquivo robots.txt
  };
  